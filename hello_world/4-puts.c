#include <stdio.h>

/**
 * main - entry point of the program
 *
 * Print a string using the function puts & return 0
 *
 * Return: always 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
