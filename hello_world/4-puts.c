#include <stdio.h>

/**
 * main - entry point of the program
 *
 * The function prints a string using puts & returns 0
 *
 * Return: always 0 (success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
