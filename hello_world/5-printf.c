#include <stdio.h>

/**
 * main - entry point
 *
 * print a string using printf & return 0
 *
 * Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
