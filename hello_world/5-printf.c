#include <stdio.h>

/**
 * main - entry point of the program
 *
 * the function prints a string using printf followed by a newline & return 0
 *
 * Return: always 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,")\n;

	return (0);
}
